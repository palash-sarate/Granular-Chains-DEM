# Test LAMMPS input script generated by Generalized Engine Stage 1
# Active parameters: dt = {{dt}}, spacing = {{spacing}}, outdir = {{outdir}}

units       si
atom_style hybrid sphere angle
boundary    f f f

# Geometry include
include {{includes_test_geometry_inc}}

# Run relaxation
print "Running relaxation for {{relax_steps}} steps with dt={{dt}}"
print "Output directory: {{outdir}}"

# Simulate dump file creation
print "Creating mock dump folder and file..."
shell mkdir -p {{outdir}}/chain
shell touch {{outdir}}/chain/chain_50000.dump

# Simulate restart binary creation
shell mkdir -p {{outdir}}/restart
shell touch {{outdir}}/restart/restart.final.bin
